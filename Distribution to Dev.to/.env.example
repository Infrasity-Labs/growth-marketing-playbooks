# Required
OPENAI_API_KEY=your-openai-key
DEVTO_API_KEY=your-devto-key

# Banner generation (optional)
# - BANNER_PROVIDER: local (default), openai, or auto (try openai then local)
BANNER_PROVIDER=local
# When using OpenAI image generation (BANNER_PROVIDER=openai or auto):
# OPENAI_IMAGE_MODEL: model identifier to request (default: dall-e-3)
OPENAI_IMAGE_MODEL=dall-e-3
# OPENAI_IMAGE_SIZE: request size sent to OpenAI (examples: 1024x1024, 1792x1024)
OPENAI_IMAGE_SIZE=1024x1024
# Optional final output size for banners (e.g., 1000x420). If set, generated
# images will be center-cropped/resized to this WxH for platform fit.
BANNER_OUTPUT_SIZE=1000x420
# Optional: dump full OpenAI image JSON response to a path for debugging
BANNER_DUMP_JSON=
# Optional caption text to render in generated banners (falls back to company blurb)
BANNER_CAPTION=

# How to make generated banners reachable by Dev.to
# Option A (recommended): upload the generated PNG to a public GitHub repo.
#   - Dev.to must be able to fetch the returned URL (repo must be PUBLIC)
#   - Create a fine-grained token with "Contents: Read and write" on that repo
BANNER_BASE_URL=
BANNER_UPLOAD_PROVIDER=github
GITHUB_TOKEN=
GITHUB_REPO=owner/repo
GITHUB_BRANCH=main
GITHUB_PATH_PREFIX=banners

# Presentation / behavior flags
# INLINE_BANNER: when set to 1/true, also embed the banner as an inline image in the markdown
INLINE_BANNER=0
# PRIMARY_KEYWORD: optional SEO keyword the summarizer will try to include
PRIMARY_KEYWORD=
# DEVTO_COMPANY_BLURB: override the default About blurb appended to published posts
DEVTO_COMPANY_BLURB=
# CANONICAL_URL: optional default canonical URL if not provided via CLI
CANONICAL_URL=

